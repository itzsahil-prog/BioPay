# BioPay Python Biometric Service
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1

# Face recognition (dlib-based ResNet — 128-D embeddings)
# Requires: cmake, dlib system libraries
# Ubuntu:  sudo apt-get install -y cmake libopenblas-dev liblapack-dev
face_recognition==1.3.0

# Voice speaker embedding (GE2E d-vector — 256-D)
resemblyzer==0.1.1.dev0

# Audio processing
soundfile==0.12.1
scipy==1.13.0

# Image processing
Pillow==10.3.0

# Numerical
numpy==1.26.4

# Cryptography (AES-256-GCM for embedding file encryption)
cryptography==42.0.7

# Optional: GPU acceleration for face_recognition
# torch==2.3.0   # Only if using GPU-accelerated models
